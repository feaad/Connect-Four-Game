from django.apps import AppConfig


# TODO: Change this app to AI
class AlgorithmConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "algorithm"
    name = "algorithm"
